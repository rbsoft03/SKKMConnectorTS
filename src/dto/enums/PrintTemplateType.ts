/**Тип печатного шаблона */
export enum PrintTemplateType{
    
    /**Реклама. */
    Advertisement = 0,

    /**Строки чека. */
    CheckLines = 1,

    /**Шапка или подвал чека. */
    HeaderOrFooter = 2
}